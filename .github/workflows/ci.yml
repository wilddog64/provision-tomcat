name: CI

on:
<<<<<<< HEAD
<<<<<<< HEAD
=======
  workflow_dispatch:
>>>>>>> 7d48ca4 (refactor(ci): combine validation and integration into single workflow)
=======
>>>>>>> 76e4837 (fix(aws): stabilize AWS integration testing pipeline and achieve parity with Azure)
  push:
    branches: [main, azure-dev, aws-dev, vagrant-dev]
    paths:
      - '**.yml'
      - '**.yaml'
      - '**.sh'
      - '**.py'
      - '**.rb'
      - 'Makefile'
      - 'tasks/**'
      - 'defaults/**'
      - 'vars/**'
      - 'roles/**'
      - 'tests/**'
      - '!docs/**'         # Exclude changes in docs folder
      - '!memory-bank/**'  # Exclude changes in memory-bank folder
  pull_request:
<<<<<<< HEAD
<<<<<<< HEAD
    branches: [main]
<<<<<<< HEAD
=======
    branches: [main, azure-dev, aws-dev, vagrant-dev]
    paths:
      - '**.yml'
      - '**.yaml'
      - '**.sh'
      - '**.py'
      - '**.rb'
      - 'Makefile'
      - 'tasks/**'
      - 'defaults/**'
      - 'vars/**'
      - 'roles/**'
      - 'tests/**'
      - '!docs/**'
      - '!memory-bank/**'
>>>>>>> 97541c1 (feat(ci): implement path filtering and add agent gh monitoring guidelines)
  workflow_dispatch:
    inputs:
      environment:
        description: 'Integration environment to run (optional, defaults to branch-based)'
        required: false
        default: 'auto'
        type: choice
        options:
          - auto
          - aws
          - azure
          - vagrant

env:
  ANSIBLE_ROLES_PATH: ${{ github.workspace }}/roles

jobs:
  validation:
    name: Validation
    runs-on: [self-hosted, macOS, ARM64]
    steps:
      - name: Debug Runner Environment
        run: |
          whoami
          pwd
          echo "HOME=$HOME"
          ls -la "$HOME" || echo "Cannot access HOME directory"
=======
=======
    branches: [main, azure-dev, aws-dev, vagrant-dev]
<<<<<<< HEAD
>>>>>>> f3a4c98 (feat(ci): switch to pull_request triggers for aws workflow)
=======
  workflow_dispatch:
>>>>>>> 76e4837 (fix(aws): stabilize AWS integration testing pipeline and achieve parity with Azure)

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  ANSIBLE_ROLES_PATH: ${{ github.workspace }}/roles

jobs:
  lint:
    name: lint
    # Security: Only run on trusted PRs/pushes/dispatches (No Forks)
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch' || github.event.pull_request.head.repo.full_name == github.repository
    runs-on: [self-hosted, macOS, ARM64]
    steps:
      - name: Debug Runner Environment
        run: |
          whoami
          pwd

      - name: Checkout provision-tomcat
        uses: actions/checkout@v4

      - name: Checkout provision-java
        uses: actions/checkout@v4
        with:
          repository: wilddog64/provision-java
          path: roles/provision-java
          token: ${{ secrets.GH_PAT || github.token }}

      - name: Checkout windows-base
        uses: actions/checkout@v4
        with:
          repository: wilddog64/windows-base
          path: roles/windows-base
          ssh-key: ${{ secrets.WINDOWS_BASE_DEPLOY_KEY }}

      - name: Checkout provision-windows-security
        uses: actions/checkout@v4
        with:
          repository: wilddog64/provision-windows-security
          path: roles/provision-windows-security
          ssh-key: ${{ secrets.PROVISION_WINDOWS_SECURITY_DEPLOY_KEY }}

      - name: Symlink self as role
        run: |
          mkdir -p roles
          ln -sfn "$PWD" roles/provision-tomcat

      - name: Verify Python Environment
        run: |
          python3 --version
          which python3

      - name: Install dependencies
        run: |
          python3 -m venv venv
          source venv/bin/activate
          python3 -m pip install --upgrade pip
          # Use stable ansible-core version
          pip install "ansible-core>=2.15.0,<2.16.0" pywinrm
          if [ -f requirements.txt ]; then python3 -m pip install -r requirements.txt; fi
          python3 -m pip install ansible-lint

      - name: Install Ansible collections
        run: |
          source venv/bin/activate
          make deps

      - name: Run Validation
<<<<<<< HEAD
        env:
          ANSIBLE_ROLES_PATH: ${{ github.workspace }}/roles
        run: make check

  vagrant-test:
    name: Vagrant Integration Tests
    needs: lint
    if: contains(github.ref, 'vagrant-dev') || contains(github.base_ref, 'vagrant-dev') || github.ref == 'refs/heads/main'
    runs-on: [self-hosted, macOS]
    env:
      ANSIBLE_ROLES_PATH: ${{ github.workspace }}/roles
      OBJC_DISABLE_INITIALIZE_FORK_SAFETY: YES
      VAGRANT_IGNORE_WINRM_PLUGIN: YES
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Cleanup Stale VMs
<<<<<<< HEAD
        run: |
<<<<<<< HEAD
          echo "Cleaning up stale VirtualBox VMs..."
          VMS=$(VBoxManage list vms | grep "kitchen-provision-tomcat" | awk -F'"' '{print $2}' || true)

          for VM in $VMS; do
            echo "Found stale VM: $VM"
            if VBoxManage showvminfo "$VM" | grep -q "State:.*running"; then
              echo "Powering off $VM..."
              VBoxManage controlvm "$VM" poweroff || true
              sleep 5
            fi
            echo "Deleting $VM..."
            VBoxManage unregistervm "$VM" --delete || true
          done
          echo "Cleanup complete."
>>>>>>> 7d48ca4 (refactor(ci): combine validation and integration into single workflow)
=======
          chmod +x bin/cleanup-vagrant.sh
          ./bin/cleanup-vagrant.sh
>>>>>>> 513b2af (ci: implement robust cleanup script for stale instances)

<<<<<<< HEAD
      - name: Checkout provision-tomcat
        uses: actions/checkout@v4

<<<<<<< HEAD
      - name: Disable Credential Helper
        run: git config --global credential.helper ""

      - name: Checkout All Roles
        run: |
          rm -rf roles
          mkdir -p roles
          # Symlink from runner dev paths (configure ANSIBLE_DEV_ROLES_PATH on self-hosted runner)
          ROLES_BASE="${ANSIBLE_DEV_ROLES_PATH:-$HOME/src/gitrepo/personal/ansible}"
          if [ -d "$ROLES_BASE/provision-java" ]; then
            ln -sfn "$ROLES_BASE/provision-java" roles/provision-java
          else
            echo "Warning: provision-java not found at $ROLES_BASE/provision-java"
          fi
          if [ -d "$ROLES_BASE/windows-base" ]; then
            ln -sfn "$ROLES_BASE/windows-base" roles/windows-base
          else
            echo "Warning: windows-base not found at $ROLES_BASE/windows-base"
          fi
          if [ -d "$ROLES_BASE/provision-windows-security" ]; then
            ln -sfn "$ROLES_BASE/provision-windows-security" roles/provision-windows-security
          else
            echo "Warning: provision-windows-security not found at $ROLES_BASE/provision-windows-security"
          fi
          # Symlink self as role
          ln -sfn .. roles/provision-tomcat

      - name: Verify Python Environment
        run: |
          python3 --version
          which python3

      - name: Install dependencies
        run: |
          python3 -m venv venv
          source venv/bin/activate
          python3 -m pip install --upgrade pip
          # Use stable ansible-core version
          pip install "ansible-core>=2.15.0,<2.16.0" pywinrm
          if [ -f requirements.txt ]; then python3 -m pip install -r requirements.txt; fi
          python3 -m pip install ansible-lint

      - name: Install Ansible collections
        run: |
          source venv/bin/activate
          make deps

      - name: Run Validation
        run: |
          source venv/bin/activate
          make check

  integration:
    name: Integration Tests (Azure or Vagrant)
    runs-on: [self-hosted, macOS, ARM64]
    needs: validation
    if: |
      (github.event_name == 'workflow_dispatch' && (github.ref_name == 'azure-dev' || github.ref_name == 'vagrant-dev')) ||
      (github.event_name == 'pull_request' && (github.head_ref == 'azure-dev' || github.head_ref == 'vagrant-dev'))
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Detect Azure Availability
        id: azure_check
        run: |
          if az account show > /dev/null 2>&1; then
            echo "AZURE_AVAILABLE=true" >> $GITHUB_ENV
            echo "SESSION_MODE=true" >> $GITHUB_ENV

            # Resolve metadata explicitly
            SUB_ID=$(az account show --query id -o tsv)
            RG_NAME=$(az group list --query "[?contains(name, 'sandbox')].name" -o tsv | head -n 1)

            echo "AZURE_SUBSCRIPTION_ID=$SUB_ID" >> $GITHUB_ENV
            az account set --subscription "$SUB_ID"

            if [ -n "$RG_NAME" ]; then
              echo "AZURE_RESOURCE_GROUP=$RG_NAME" >> $GITHUB_ENV
              echo "Azure session detected. Sub: $SUB_ID, RG: $RG_NAME"
            else
              # Fallback RG name if sandbox cannot be listed
              echo "AZURE_RESOURCE_GROUP=kqvm-win11-rg" >> $GITHUB_ENV
              echo "Azure session detected. Sub: $SUB_ID, RG fallback used: kqvm-win11-rg"
            fi
          elif [ -n "${{ secrets.AZURE_CLIENT_ID }}" ]; then
            echo "AZURE_AVAILABLE=true" >> $GITHUB_ENV
            echo "SESSION_MODE=false" >> $GITHUB_ENV
            echo "Azure secrets detected."
          else
            echo "AZURE_AVAILABLE=false" >> $GITHUB_ENV
            echo "No Azure session or secrets found."
          fi

      - name: Checkout All Roles
        run: |
          rm -rf roles
          mkdir -p roles
          # Symlink from runner dev paths (configure ANSIBLE_DEV_ROLES_PATH on self-hosted runner)
          ROLES_BASE="${ANSIBLE_DEV_ROLES_PATH:-$HOME/src/gitrepo/personal/ansible}"
          if [ -d "$ROLES_BASE/provision-java" ]; then
            ln -sfn "$ROLES_BASE/provision-java" roles/provision-java
          else
            echo "Warning: provision-java not found at $ROLES_BASE/provision-java"
          fi
          if [ -d "$ROLES_BASE/windows-base" ]; then
            ln -sfn "$ROLES_BASE/windows-base" roles/windows-base
          else
            echo "Warning: windows-base not found at $ROLES_BASE/windows-base"
          fi
          if [ -d "$ROLES_BASE/provision-windows-security" ]; then
            ln -sfn "$ROLES_BASE/provision-windows-security" roles/provision-windows-security
          else
            echo "Warning: provision-windows-security not found at $ROLES_BASE/provision-windows-security"
          fi

      - name: Symlink current role
        run: |
          mkdir -p roles
          ln -sfn .. roles/provision-tomcat

      - name: Install Ansible collections
        run: |
          python3 -m venv venv
          source venv/bin/activate
          make deps

      - name: Install Ruby Dependencies
        run: |
          source venv/bin/activate
          echo "Checking for rbenv..."
          if [ -d "$HOME/.rbenv" ]; then
            RBENV_ROOT="$HOME/.rbenv"
          elif [ -d "/usr/local/var/rbenv" ]; then
            RBENV_ROOT="/usr/local/var/rbenv"
          elif [ -d "/opt/homebrew/var/rbenv" ]; then
            RBENV_ROOT="/opt/homebrew/var/rbenv"
          fi

          if [ -n "$RBENV_ROOT" ]; then
            echo "Found rbenv at $RBENV_ROOT"
            export PATH="$RBENV_ROOT/shims:$RBENV_ROOT/bin:$PATH"
            export RBENV_SHELL=bash
            bundle -v || gem install bundler
            bundle install
          else
            echo "rbenv not found, trying system ruby/bundle"
            bundle install || gem install bundler && bundle install
          fi

      - name: Login to Azure
        if: env.AZURE_AVAILABLE == 'true' && env.SESSION_MODE == 'false'
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Run Azure Test
        if: env.AZURE_AVAILABLE == 'true'
        env:
          OBJC_DISABLE_INITIALIZE_FORK_SAFETY: "YES"
        run: |
          source venv/bin/activate
          ansible --version
          make test-azure-provision-tomcat

      - name: Pre-download Vagrant Box
        if: env.AZURE_AVAILABLE == 'false'
        run: |
          source venv/bin/activate
          ./bin/vagrant-wrapper box add --provider virtualbox --force gusztavvargadr/windows-11-22h2-pro-arm64 || true

      - name: Fallback to Vagrant Test
        if: env.AZURE_AVAILABLE == 'false' && false
        env:
          OBJC_DISABLE_INITIALIZE_FORK_SAFETY: "YES"
        run: |
          source venv/bin/activate
          make test-provision-tomcat

  aws_integration:
    name: AWS Integration Tests
    runs-on: ubuntu-latest
    needs: validation
    if: |
      (github.event_name == 'workflow_dispatch' && github.ref_name == 'aws-dev') ||
      (github.event_name == 'pull_request' && github.head_ref == 'aws-dev')
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run AWS Test
        run: echo "AWS tests not yet implemented"
=======
=======
>>>>>>> 97314b9 (ci: fix step order - checkout before cleanup)
      - name: Checkout provision-java
=======
        run: ./bin/cleanup-vagrant.sh
      - name: Run Vagrant Tests
        run: bundle exec make test-upgrade-baseline-win11

  azure-test:
    name: Azure Integration Tests
    needs: lint
    if: contains(github.ref, 'azure-dev') || contains(github.base_ref, 'azure-dev')
    runs-on: [self-hosted, macOS]
    steps:
      - name: Checkout
>>>>>>> f3a4c98 (feat(ci): switch to pull_request triggers for aws workflow)
        uses: actions/checkout@v4
      - name: Login to Azure
=======
>>>>>>> 76e4837 (fix(aws): stabilize AWS integration testing pipeline and achieve parity with Azure)
        run: |
          source venv/bin/activate
          make check

  aws_integration:
    name: AWS Integration Tests
    runs-on: [self-hosted, macOS, ARM64]
    needs: lint
    if: |
      (github.event_name == 'push' || github.event_name == 'workflow_dispatch' || github.event.pull_request.head.repo.full_name == github.repository) && (
        (github.event_name == 'push' && (github.ref == 'refs/heads/aws-dev' || github.ref == 'refs/heads/main')) ||
        (github.event_name == 'pull_request' && github.event.pull_request.draft == false && (github.base_ref == 'main' || github.base_ref == 'aws-dev' || github.head_ref == 'aws-dev')) ||
        (github.event_name == 'workflow_dispatch' && inputs.environment == 'aws')
      )
    outputs:
      AWS_AVAILABLE: ${{ steps.aws_check.outputs.AWS_AVAILABLE }}
    steps:
      - name: Checkout provision-tomcat
        uses: actions/checkout@v4
<<<<<<< HEAD
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
<<<<<<< HEAD
          repository: wilddog64/provision-java
          path: roles/provision-java

      - name: Hotpatch provision-java
        run: |
          python3 -c "
          import os
          fpath = 'roles/provision-java/tasks/install-Windows-java.yml'
          if os.path.exists(fpath):
              with open(fpath, 'r') as f: content = f.read()

              content = content.replace('regex_search', 'regex_findall')
              content = content.replace('openjdk version', 'version')
              content = content.replace(\", '\\\\1'\", \"\")
              content = content.replace(\", '\\1'\", \"\")
              content = content.replace('java_version_output.stderr', '(java_version_output.stdout + java_version_output.stderr)')

              with open(fpath, 'w') as f: f.write(content)
              print('Hotpatched provision-java task with robust regex_findall fix')
          "

      - name: Install SSH Keys and Config
        run: |
          mkdir -p ~/.ssh

          echo "${{ secrets.WINDOWS_BASE_DEPLOY_KEY }}" > ~/.ssh/id_windows_base
          chmod 600 ~/.ssh/id_windows_base

          echo "${{ secrets.PROVISION_WINDOWS_SECURITY_DEPLOY_KEY }}" > ~/.ssh/id_provision_security
          chmod 600 ~/.ssh/id_provision_security

          cat <<EOF > ~/.ssh/config
          Host windows-base-repo
            HostName github.com
            User git
            IdentityFile ~/.ssh/id_windows_base
            IdentitiesOnly yes

          Host provision-security-repo
            HostName github.com
            User git
            IdentityFile ~/.ssh/id_provision_security
            IdentitiesOnly yes
          EOF

          ssh-keyscan github.com >> ~/.ssh/known_hosts
          chmod 700 ~/.ssh

      - name: Checkout windows-base
        run: git clone windows-base-repo:wilddog64/windows-base.git roles/windows-base

      - name: Checkout provision-windows-security
        run: git clone provision-security-repo:wilddog64/provision-windows-security.git roles/provision-windows-security

      - name: Install Dependencies
        run: |
          python3 -m venv .venv
          source .venv/bin/activate
          pip install -r requirements.txt

          bundle config set --local path 'vendor/bundle'
          bundle install

          export PATH=$PWD/.venv/bin:$PATH

      - name: Workaround Vagrant Bundler Conflict
        run: |
          mkdir -p $HOME/.local/bin
          cat <<'EOF' > $HOME/.local/bin/vagrant
          #!/bin/bash
          ENV_ARGS=(
            HOME="$HOME"
            PATH="/opt/vagrant/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin"
            TERM="${TERM:-dumb}"
            LANG="${LANG:-en_US.UTF-8}"
            USER="$USER"
            SHELL="$SHELL"
            TMPDIR="${TMPDIR:-/tmp}"
          )

          [[ -n "${SSH_AUTH_SOCK:-}" ]] && ENV_ARGS+=( SSH_AUTH_SOCK="$SSH_AUTH_SOCK" )
          [[ -n "${VAGRANT_CWD:-}" ]]  && ENV_ARGS+=( VAGRANT_CWD="$VAGRANT_CWD" )

          exec /usr/bin/env -i "${ENV_ARGS[@]}" /usr/local/bin/vagrant "$@"
          EOF
          chmod +x $HOME/.local/bin/vagrant

          ln -sf $PWD/.venv/bin/ansible-playbook $HOME/.local/bin/ansible-playbook
          ln -sf $PWD/.venv/bin/ansible-galaxy $HOME/.local/bin/ansible-galaxy

          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Run Test (Upgrade Baseline Win11)
        timeout-minutes: 20
        run: |
          source .venv/bin/activate

          python3 - <<'EOF'
          import os, re, sys

          ansible_path = os.path.abspath('.venv/bin/ansible-playbook')
          if not os.path.exists(ansible_path):
              print(f"Error: {ansible_path} not found")
              sys.exit(1)

          for kfile in ['.kitchen.yml', '.kitchen-win.yml']:
              if not os.path.exists(kfile): continue
              with open(kfile, 'r') as f: content = f.read()

              if 'ansible_playbook_bin:' in content:
                  content = re.sub(r'ansible_playbook_bin:.*', f'ansible_playbook_bin: {ansible_path}', content)
              else:
                  content = content.replace('name: ansible_push', f'name: ansible_push\n      ansible_playbook_bin: {ansible_path}')

              content = content.replace('connection_retries: 5', 'connection_retries: 30')

              with open(kfile, 'w') as f: f.write(content)
              print(f"Patched {kfile}")
          EOF

          echo "=== Verification of Patched .kitchen.yml ==="
          grep "ansible_playbook_bin" .kitchen.yml | head -n 5
          grep "connection_retries" .kitchen.yml

          bundle exec make test-upgrade-baseline-win11

      - name: Post-Test Cleanup
        if: always()
        run: |
<<<<<<< HEAD
          echo "Performing final VirtualBox cleanup..."
          VMS=$(VBoxManage list vms | grep "kitchen-provision-tomcat" | awk -F'"' '{print $2}' || true)
          for VM in $VMS; do
            echo "Cleaning up $VM..."
            VBoxManage controlvm "$VM" poweroff || true
            sleep 2
            VBoxManage unregistervm "$VM" --delete || true
          done
>>>>>>> 7d48ca4 (refactor(ci): combine validation and integration into single workflow)
=======
          ./bin/cleanup-vagrant.sh
>>>>>>> 513b2af (ci: implement robust cleanup script for stale instances)
=======
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1
      - name: Run AWS Test
        run: make test-aws-provision-tomcat
>>>>>>> f3a4c98 (feat(ci): switch to pull_request triggers for aws workflow)
=======

      - name: Configure AWS Credentials
        if: |
          github.ref == 'refs/heads/aws-dev' ||
          github.ref == 'refs/heads/main' ||
          github.base_ref == 'main' ||
          github.base_ref == 'aws-dev' ||
          (github.event_name == 'workflow_dispatch' && inputs.environment == 'aws')
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
          aws-region: us-east-1

      - name: Verify AWS Availability
        if: |
          github.ref == 'refs/heads/aws-dev' ||
          github.ref == 'refs/heads/main' ||
          github.base_ref == 'main' ||
          github.base_ref == 'aws-dev' ||
          (github.event_name == 'workflow_dispatch' && inputs.environment == 'aws')
        id: aws_check
        run: |
          if aws sts get-caller-identity > /dev/null 2>&1; then
            echo "AWS_AVAILABLE=true" >> $GITHUB_ENV
            echo "AWS_AVAILABLE=true" >> $GITHUB_OUTPUT
            echo "AWS session verified."
          else
            echo "AWS_AVAILABLE=false" >> $GITHUB_ENV
            echo "AWS_AVAILABLE=false" >> $GITHUB_OUTPUT
            echo "AWS session invalid."
          fi

      - name: Discover Dynamic AWS Resources
        if: steps.aws_check.outputs.AWS_AVAILABLE == 'true'
        id: discover_resources
        run: |
          # Call the make target to discover resources and save to a file
          make discover-aws-resources --silent > aws_env.txt
          
          # Safely parse and export to GITHUB_ENV
          # Only allows keys matching the AWS_[A-Z0-9_]+ pattern
          while IFS='=' read -r key value; do
            if [[ $key =~ ^AWS_[A-Z0-9_]+$ ]]; then
              echo "Exporting $key"
              echo "$key=$value" >> $GITHUB_ENV
            fi
          done < aws_env.txt
          rm aws_env.txt

      - name: Authorize Security Group Ingress
        if: steps.aws_check.outputs.AWS_AVAILABLE == 'true'
        run: |
          # Restrict CIDR to the runner's public IP for security
          RUNNER_IP=$(curl -s https://checkip.amazonaws.com)
          echo "RUNNER_IP=${RUNNER_IP}" >> $GITHUB_ENV
          echo "Authorizing ingress for runner IP: $RUNNER_IP"
          aws ec2 authorize-security-group-ingress --group-id $AWS_SECURITY_GROUP_ID --protocol tcp --port 5985 --cidr ${RUNNER_IP}/32 || true
          aws ec2 authorize-security-group-ingress --group-id $AWS_SECURITY_GROUP_ID --protocol tcp --port 8080 --cidr ${RUNNER_IP}/32 || true
          aws ec2 authorize-security-group-ingress --group-id $AWS_SECURITY_GROUP_ID --protocol tcp --port 9080 --cidr ${RUNNER_IP}/32 || true

      - name: Checkout Roles
        if: steps.aws_check.outputs.AWS_AVAILABLE == 'true'
        run: |
          mkdir -p roles
          ln -sfn "$PWD" roles/provision-tomcat

      - name: Checkout provision-java
        if: steps.aws_check.outputs.AWS_AVAILABLE == 'true'
        uses: actions/checkout@v4
        with:
          repository: wilddog64/provision-java
          path: roles/provision-java
          token: ${{ secrets.GH_PAT || github.token }}

      - name: Checkout windows-base
        if: steps.aws_check.outputs.AWS_AVAILABLE == 'true'
        uses: actions/checkout@v4
        with:
          repository: wilddog64/windows-base
          path: roles/windows-base
          ssh-key: ${{ secrets.WINDOWS_BASE_DEPLOY_KEY }}

      - name: Checkout provision-windows-security
        if: steps.aws_check.outputs.AWS_AVAILABLE == 'true'
        uses: actions/checkout@v4
        with:
          repository: wilddog64/provision-windows-security
          path: roles/provision-windows-security
          ssh-key: ${{ secrets.PROVISION_WINDOWS_SECURITY_DEPLOY_KEY }}

      - name: Install dependencies
        if: steps.aws_check.outputs.AWS_AVAILABLE == 'true'
        run: |
          python3 -m venv venv
          source venv/bin/activate
          python3 -m pip install --upgrade pip
          pip install "ansible-core>=2.15.0,<2.16.0" pywinrm
          make deps

      - name: Install Ruby Dependencies
        if: steps.aws_check.outputs.AWS_AVAILABLE == 'true'
        run: |
          source venv/bin/activate
          bundle install || gem install bundler && bundle install

      - name: Run AWS Test
        if: steps.aws_check.outputs.AWS_AVAILABLE == 'true'
        env:
          OBJC_DISABLE_INITIALIZE_FORK_SAFETY: "YES"
        run: |
          source venv/bin/activate
          echo "=== Diagnostic: Verifying AMI Visibility ==="
          aws ec2 describe-images --image-ids $AWS_AMI_ID --region us-east-1 --query "Images[0].ImageId" || echo "AMI NOT FOUND OR ACCESS DENIED"
          ansible --version
          make test-aws-provision-tomcat

      - name: Mandatory Cleanup
        if: always() && steps.aws_check.outputs.AWS_AVAILABLE == 'true'
        run: |
          source venv/bin/activate || true
          bundle exec kitchen destroy default-aws-minimal-win-disk || true
          bundle exec kitchen destroy upgrade-candidate-aws-disk-aws-minimal-win-disk || true
          
          # Revoke security group ingress after test completion
          # Re-using RUNNER_IP from environment to ensure consistency
          echo "Revoking ingress for runner IP: $RUNNER_IP"
          aws ec2 revoke-security-group-ingress --group-id $AWS_SECURITY_GROUP_ID --protocol tcp --port 5985 --cidr ${RUNNER_IP}/32 || true
          aws ec2 revoke-security-group-ingress --group-id $AWS_SECURITY_GROUP_ID --protocol tcp --port 8080 --cidr ${RUNNER_IP}/32 || true
          aws ec2 revoke-security-group-ingress --group-id $AWS_SECURITY_GROUP_ID --protocol tcp --port 9080 --cidr ${RUNNER_IP}/32 || true

  azure_integration:
    name: Azure Integration Tests
    runs-on: [self-hosted, macOS, ARM64]
    needs: lint
    if: >-
      (github.event_name == 'push' || github.event_name == 'workflow_dispatch' || github.event.pull_request.head.repo.full_name == github.repository) && (
        (github.event_name == 'pull_request' && github.event.pull_request.draft == false && github.head_ref == 'azure-dev') ||
        (github.event_name == 'push' && github.ref_name == 'azure-dev') ||
        (github.event_name == 'workflow_dispatch' && (github.ref_name == 'azure-dev' || inputs.environment == 'azure'))
      )
    steps:
      - name: Checkout provision-tomcat
        uses: actions/checkout@v4

      - name: Detect Azure Availability
        run: |
          if az account show > /dev/null 2>&1; then
            echo "AZURE_AVAILABLE=true" >> $GITHUB_ENV
            echo "Azure session detected."
          else
            echo "AZURE_AVAILABLE=false" >> $GITHUB_ENV
          fi

      - name: Checkout Roles
        if: env.AZURE_AVAILABLE == 'true'
        run: |
          mkdir -p roles
          ln -sfn "$PWD" roles/provision-tomcat

      - name: Checkout provision-java
        if: env.AZURE_AVAILABLE == 'true'
        uses: actions/checkout@v4
        with:
          repository: wilddog64/provision-java
          path: roles/provision-java
          token: ${{ secrets.GH_PAT || github.token }}

      - name: Checkout windows-base
        if: env.AZURE_AVAILABLE == 'true'
        uses: actions/checkout@v4
        with:
          repository: wilddog64/windows-base
          path: roles/windows-base
          ssh-key: ${{ secrets.WINDOWS_BASE_DEPLOY_KEY }}

      - name: Checkout provision-windows-security
        if: env.AZURE_AVAILABLE == 'true'
        uses: actions/checkout@v4
        with:
          repository: wilddog64/provision-windows-security
          path: roles/provision-windows-security
          ssh-key: ${{ secrets.PROVISION_WINDOWS_SECURITY_DEPLOY_KEY }}

      - name: Run Azure Test
        if: env.AZURE_AVAILABLE == 'true'
        run: make test-azure-provision-tomcat

  vagrant_integration:
    name: Vagrant Integration Tests
    runs-on: [self-hosted, macOS, ARM64]
    needs: [lint, aws_integration]
    if: |
      always() &&
      needs.lint.result == 'success' &&
      (github.event_name == 'push' || github.event_name == 'workflow_dispatch' || github.event.pull_request.head.repo.full_name == github.repository) &&
      (
        (github.event_name == 'pull_request' && github.event.pull_request.draft == false && github.head_ref == 'vagrant-dev') ||
        (github.event_name == 'push' && github.ref_name == 'vagrant-dev') ||
        (github.event_name == 'workflow_dispatch' && (github.ref_name == 'vagrant-dev' || inputs.environment == 'vagrant')) ||
        (github.ref == 'refs/heads/main' && (needs.aws_integration.result == 'skipped' || needs.aws_integration.outputs.AWS_AVAILABLE == 'false'))
      )
    steps:
      - name: Checkout provision-tomcat
        uses: actions/checkout@v4
      - name: Run Vagrant Test
        run: |
<<<<<<< HEAD
          # Disabled due to instability on Apple Silicon
          echo "Vagrant tests are currently disabled on Apple Silicon runners."
<<<<<<< HEAD
          exit 0
>>>>>>> 76e4837 (fix(aws): stabilize AWS integration testing pipeline and achieve parity with Azure)
=======
          exit 0
>>>>>>> a69e2cc (ci: implement portable role checkouts using DEPLOY_KEY secrets)
=======
          echo "AWS Sandbox not available. Falling back to Vagrant tests..."
          # Note: Currently disabled due to Apple Silicon instability, but logic is now conditional
          exit 0
>>>>>>> 68e32dd (feat(ci): implement dynamic secret sync and conditional vagrant fallback)
