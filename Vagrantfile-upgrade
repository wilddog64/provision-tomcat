# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure("2") do |config|
  config.vm.box = 'windows11-tomcat112'

  config.vm.communicator = "winrm"
  config.winrm.username = "vagrant"
  config.winrm.password = "vagrant"
  config.winrm.transport = :plaintext
  config.winrm.basic_auth_only = true

  # Add secondary disk for D: drive
  config.vm.provider "virtualbox" do |vb|
    disk_file = File.join(File.dirname(__FILE__), ".vagrant", "upgrade_data_disk.vdi")

    # Clean up stale VirtualBox registration if file doesn't exist but is still registered
    unless File.exist?(disk_file)
      vbox_list = `VBoxManage list hdds 2>/dev/null`
      if vbox_list.include?(disk_file)
        uuid_match = vbox_list.match(/UUID:\s+([a-f0-9-]+).*?Location:\s+#{Regexp.escape(disk_file)}/m)
        if uuid_match
          uuid = uuid_match[1]
          # Check if disk is attached to any VM before closing
          attached_vms = `VBoxManage showhdinfo #{uuid} 2>/dev/null | grep 'In use by VMs:'`.strip
          if attached_vms.empty? || attached_vms.include?('In use by VMs:')
            system("VBoxManage closemedium disk #{uuid} --delete 2>/dev/null")
          else
            warn "Warning: Disk #{disk_file} is attached to a VM. Skipping cleanup."
          end
        end
      end
      vb.customize ['createhd', '--filename', disk_file, '--size', 50 * 1024]
    end
    
    # Verify the disk isn't already attached before attaching
    vm_info = `VBoxManage showvminfo #{vb.name || 'provision-tomcat'} --machinereadable 2>/dev/null` rescue ""
    unless vm_info.include?(disk_file)
      vb.customize ['storageattach', :id, '--storagectl', 'SATA Controller',
                    '--port', 1, '--device', 0, '--type', 'hdd', '--medium', disk_file]
    end
  end

  # Initialize and format D: drive
  config.vm.provision "disk_setup", type: "shell" do |s|
    s.inline = <<-POWERSHELL
      $disk = Get-Disk | Where-Object PartitionStyle -eq 'RAW'
      if ($disk) {
        Write-Host "Initializing and formatting D: drive..."
        $disk | Initialize-Disk -PartitionStyle GPT -PassThru |
          New-Partition -DriveLetter D -UseMaximumSize |
          Format-Volume -FileSystem NTFS -NewFileSystemLabel "Data" -Confirm:$false
        Write-Host "D: drive created successfully"
      } else {
        Write-Host "No RAW disk found or D: drive already exists"
      }
    POWERSHELL
  end

  common_env = {
    'ansible_connection'                   => 'winrm',
    'ansible_winrm_transport'              => 'basic',
    'ansible_winrm_server_cert_validation' => 'ignore',
    'ansible_winrm_scheme'                 => 'http',
    'install_drive'                        => 'D:',
  }

  config.vm.network "forwarded_port", guest: 8080, host: 8080
  config.vm.network "forwarded_port", guest: 9080, host: 9080

  config.vm.provision 'ansible_upgrade_step2_prepare', type: :ansible, run: 'never' do |ansible|
    ansible.limit = 'all'
    ansible.galaxy_role_file = 'requirements.yml'
    ansible.playbook = 'tests/playbook-upgrade.yml'
    ansible.extra_vars = common_env.merge(
      'upgrade_step' => 2,
      'tomcat_auto_start' => true,
      'tomcat_candidate_enabled' => true,
      'tomcat_candidate_delegate' => 'localhost',
      'tomcat_candidate_manual_control' => true
    )
  end

  config.vm.provision 'ansible_upgrade_step2_finalize', type: :ansible, run: 'never' do |ansible|
    ansible.limit = 'all'
    ansible.galaxy_role_file = 'requirements.yml'
    ansible.playbook = 'tests/playbook-upgrade.yml'
    ansible.extra_vars = common_env.merge(
      'upgrade_step' => 2,
      'tomcat_auto_start' => true,
      'tomcat_candidate_enabled' => true,
      'tomcat_candidate_delegate' => 'localhost',
      'tomcat_candidate_manual_control' => false
    )
  end
end