#SPDX-License-Identifier: MIT-0
---
# defaults file for provision-tomcat
tomcat_package_name: tomcat
tomcat_service_name: Tomcat9
tomcat_state: present  # Use 'latest' to ensure upgrades to newest version
tomcat_version: null   # Set to specific version if available in repo (check with: choco search tomcat --all-versions)
tomcat_choco_args: []
